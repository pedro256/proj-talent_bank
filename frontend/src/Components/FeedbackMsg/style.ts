import styled from 'styled-components'
import { ColorsPallet } from '../../Styles/colors'

export const TextFeedback = styled.h3`
    margin:18px;
    text-align: center;
    color: ${ColorsPallet.softBlack};
    font-weight:bold;
    text-transform: uppercase;
    
`